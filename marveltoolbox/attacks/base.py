import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F


class Attack:
    def __init__(self):
        pass

    def attack_batch(self, model, inputs, labels):
        return inputs, labels
